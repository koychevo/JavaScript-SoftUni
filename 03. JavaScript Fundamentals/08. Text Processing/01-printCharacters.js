function printCharacters(str) {
    for(let ch of str) {
        console.log(ch);
    }
}

printCharacters('AWord');

/*
Task: Write a function that receives a string and prints all the characters on separate lines.
*/
