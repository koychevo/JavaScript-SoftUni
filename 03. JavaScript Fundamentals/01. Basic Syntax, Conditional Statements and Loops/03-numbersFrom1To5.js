function printNumbers() {
    for(let num = 1; num <= 5; num++) {
        console.log(num);
    }
} 

printNumbers();

/*
Task: Create a function that prints all the numbers from 1 to 5 (inclusive) each on a separate line
*/
