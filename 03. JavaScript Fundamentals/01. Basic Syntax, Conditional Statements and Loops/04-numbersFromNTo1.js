function printNumbers(number) {
    let num = Number(number);
    while(num > 0) {
        console.log(num--);
    }
}

printNumbers(5);
printNumbers(3);

/*
Task: Create a function that receives a number N and prints all the numbers from N to 1. Try using while loop
*/